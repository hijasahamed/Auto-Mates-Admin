import 'package:flutter/material.dart';

class BlockedSellersScreen extends StatelessWidget {
  const BlockedSellersScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('sini'),
    );
  }
}